# robots.txt for Project and Task Manager

# Allow all web crawlers to access the site
User-agent: *

# Disallow specific paths if needed (add paths as needed)
# Disallow: /admin

# Allow all other paths
Disallow:

# Sitemap location (add your sitemap URL when available)
# Sitemap: https://yourdomain.com/sitemap.xml
